import 'package:flutter/cupertino.dart';

class ListViewCalls extends StatelessWidget {
  const ListViewCalls({super.key});

  @override
  Widget build(BuildContext context) {
    return const CupertinoPageScaffold(child: Text("No Data"));
  }
}
